<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kipras.newmafija">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".app.StartPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".app.CreateGame"/>
        <activity android:name=".app.Game"/>
        <activity android:name=".app.Night"/>
        <activity android:name=".app.ReportNight"/>
        <activity android:name=".app.Day"/>
        <activity android:name=".app.ReportDay"/>

    </application>

</manifest>